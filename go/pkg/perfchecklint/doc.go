// Package perfchecklint exposes the public perfcheck analyzer set and metadata
// so external tooling (e.g., GolangCI-Lint) can embed the suite without relying
// on internal packages.
package perfchecklint
